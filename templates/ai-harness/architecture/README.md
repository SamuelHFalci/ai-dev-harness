# Architecture notes (local)

Markdown referenced by **`.ai-harness/architecture-rules.mdc`** lives here. This tree is under **`.ai-harness/`**, so it is **not** committed when you gitignore `/.ai-harness/`.
